services:
  - type: web
    name: strava-coach-poc
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn run:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
